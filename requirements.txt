python-docx==1.1.0        # For high-level Word document parsing and writing
lxml==5.2.1               # For low-level XML parsing inside DOCX
fuzzywuzzy==0.18.0        # For matching segments using fuzzy logic
python-Levenshtein==0.23.0 # Improves fuzzywuzzy performance
# docxcompose==1.3.9      # Optional: For combining and comparing docx documents 